#include "textures2D.h"
#include <gfx/include/gfx.h>

namespace common {
	
//Diligent::RefCntAutoPtr<Diligent::ITexture> loadTexture(Diligent::RefCntAutoPtr<Diligent::IRenderDevice> _device, const char* _filePath) {
//	Diligent::TextureLoadInfo loadInfo;
//	loadInfo.IsSRGB = true;
//
//	return Chowder::loadTexture(_filePath, loadInfo, _device);
//}

}